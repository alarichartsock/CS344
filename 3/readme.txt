To compile:

gcc -std=gnu99 smallsh.c -o smallsh

To run:

chmod +x smallsh // If you don't have execute permissions
./smallsh

All other program requirements apply. To exit, type exit in the prompt.
